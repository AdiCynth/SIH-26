# URL for the local VibeGuard backend. Override to point to a remote API if needed.
NEXT_PUBLIC_API_URL=http://localhost:8000

# When true, the frontend uses the bundled demo implementations instead of the real backend.
# Set to "true" to run the demo mode; leave as "false" (default) to use the real backend.
NEXT_PUBLIC_DEMO_MODE=false
